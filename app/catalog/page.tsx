import {getCatalog} from '@/lib/catalog';import Link from 'next/link';export const dynamic='force-dynamic';export default async function Catalog(){const items=await getCatalog();return(<main className='container py-8'><div className='flex items-end justify-between gap-4'><div><h2 className='text-xl font-semibold'>Catalog</h2><p className='text-sm muted'>Prices are in USD (FCA Miami).</p></div><Link className='btn' href='/rfq'>Request a Quote</Link></div><div className='grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6'>{items.map((p:any)=>(<Link href={`/product/${encodeURIComponent(p.SKU)}`} key={p.SKU} className='card p-4 block'><div className='text-sm muted'>{p.Brand} • {p.Category}</div><div className='font-semibold'>{p.Model}</div><div className='grid grid-cols-2 gap-1 text-sm mt-2'><div>CPU: {p.CPU}</div><div>RAM: {p['RAM (GB)']}GB</div><div>Storage: {p.Storage}</div><div>Grade: {p['Condition Grade']}</div><div>Qty: {p['Qty Available']}</div><div>SKU: {p.SKU}</div></div><div className='mt-3 font-bold'>${typeof p['Your Price USD']==='number'?p['Your Price USD'].toFixed(2):p['Your Price USD']}</div></Link>))}</div></main>)}